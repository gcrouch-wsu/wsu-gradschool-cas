/** Defaults when publication-specific public fields are blank. */

export const DEFAULT_PUBLIC_HEADER_TITLE = "GradCAS and EngineeringCAS Applications";

/** Secondary line under the title (often the application cycle). */
export const DEFAULT_PUBLIC_HEADER_SUBTITLE = "Washington State University";

export const DEFAULT_PUBLIC_HEADER_TITLE_HREF = "/";

export const DEFAULT_PUBLIC_HERO_EYEBROW = "Program view";

export const DEFAULT_PUBLIC_HERO_BODY =
  "Search or select a degree or certificate to review requirements and materials from the published CAS export. While you have text in the search box, the list is ordered with stronger matches first and the top match is selected automatically.";
